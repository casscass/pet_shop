<%= form_with(model: sevice, local: true) do |form| %>
  <% if sevice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sevice.errors.count, "error") %> prohibited this sevice from being saved:</h2>

      <ul>
      <% sevice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :sevice_name %>
  </div>

  <div class="field">
    <%= form.label :cost %>
    <%= form.text_field :cost, id: :sevice_cost %>
  </div>

  <div class="field">
    <%= form.label :time %>
    <%= form.text_field :time, id: :sevice_time %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
